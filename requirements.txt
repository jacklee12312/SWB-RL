# Runtime dependencies for swb-rl.
#
# The engine currently uses only the Python standard library.
# Install with:
#   python -m pip install -r requirements.txt
#
# Python itself is specified in pyproject.toml as >=3.11.
